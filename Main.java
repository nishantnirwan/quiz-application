/**
 * Main.java — Entry point of the Quiz Application
 * Just creates a QuizApp object and starts the quiz.
 */
public class Main {
    public static void main(String[] args) {
        QuizApp quiz = new QuizApp();
        quiz.startQuiz();
    }
}
